# Vanova Redesign — Master Checklist

Source: `UI_UX_AUDIT.md` (live-screen audit, supersedes/extends the source-read-only pass). Every item from both audit passes is represented here. Working top to bottom, one item at a time. Checking items off as they land; nothing skipped without saying so explicitly.

Legend: `[ ]` not started · `[~]` in progress · `[x]` done

---

## FIX IMMEDIATELY

- [x] 1. Homepage blank first paint — `Reveal.tsx` starts content at `opacity-0`, only visible after `IntersectionObserver`. Hero/first viewport must render visible by default. **Done:** added `eager` prop to `Reveal` (skips the observer/opacity state, renders immediately); applied to the two hero-section `Reveal`s in `app/page.tsx` (headline+CTA, live-demo panel). Below-the-fold sections keep the scroll-triggered reveal.
- [x] 2. Public mobile hero/header crowding — `/[slug]` mobile hero has description + title + hours + 2 buttons + floating chat + sticky header all stacked; hero image too dark/muddy; business name truncates. **Done:** hero was `h-[420px]` (fixed) + `overflow-hidden` — a business with a description, wrapping name, and hours line could genuinely get clipped on a short phone. Switched to `min-h-[…]` so it grows instead of clipping. Description clamps to 2 lines on mobile only; tightened vertical rhythm (`mt-5/mt-6` instead of `mt-6/mt-8`); h1 32px on mobile. Header: smaller logo/name/button chrome on mobile buys the business name real room before its `truncate` ellipsis kicks in. (Hero image darkness/crop is a per-business asset choice, not a code fix — left for #19.)
- [x] 3. First-run setup checklist on dashboard — `Profile → Services → Hours → Booking rules → Preview → Share`, each with completion state, direct action, live preview. **Done:** new `SetupChecklist` component — Profile/Services/Hours as required steps (done-signal: description-or-logo set, ≥1 active service, ≥1 default hours row), Payment listed but optional (never blocks). Progress bar + per-step "Set up"/"Edit" links to the right settings section. Preview-your-page + copy-booking-link footer. **Disappears entirely once the 3 required steps are done** — no permanent onboarding chrome on a screen a mature business opens daily.
- [x] 4. Dashboard next-appointment / today's-queue visual dominance — turn "Next appointment" into a real actionable card (contact, reschedule, cancel, complete, no-show); today = queue not count. Also from the live-screen pass: icon-only Copy-link/Export actions are too ambiguous (need a visible label at larger sizes); mobile summary card is too tall/low-density; mobile order should be header → next appointment → today's schedule → quick actions → performance summary → assistant. **Done:** new `NextAppointmentCard` (full day+time, customer, service, Message/Call, Reschedule → deep-links into the AI assistant pre-filled so it goes through the existing confirm-before-change trust flow, Mark completed, No-show, Copy reschedule link, Cancel) backed by new staff-authenticated `POST /api/bookings/[id]/status` (business-scoped, distinct from the customer-facing self-serve cancel route). Stats strip dropped from 4 to 3 cells (Today / Today's revenue / This week) in a proper 3-col grid on mobile instead of wrapping. `DashboardHeaderActions` now shows a visible "Copy link"/"Export CSV" label from `sm:` up. Order is now: header → setup checklist (if incomplete) → next appointment → stats → assistant → list.
- [x] 5. "Next up" must show the date, not just time (currently reads as today when it may be days out) — also true of the summary line under it. **Done as part of #4** — `NextAppointmentCard` always shows a full day label (`Today`/`Tomorrow`/`Tue, Aug 25`) plus time, replacing the old time-only "Next up" stat tile.
- [x] 6. Replace/hide the `/shop` "design pending" placeholder — must not be reachable in production as-is. **Done:** confirmed nothing links to it (no nav, no middleware rewrite) — it's an orphaned scaffold. Route now calls `notFound()` rather than rendering the placeholder text or inventing a "coming soon" page for a feature that isn't actually scoped yet.

## FIX NEXT

- [x] 7. Admin nav regroup: **Today** (Dashboard/Calendar/Customers) · **Set up** (Services/Hours/Staff/Booking page) · **Automate** (Assistant/Channels) · **Business** (Insights/Billing/Settings). Apply to `AdminSidebar.tsx` and `AdminMobileNav.tsx`; add status indicators (setup incomplete, channel disconnected, trial ending, pending schedule changes). Also fix the 766px dead-zone breakpoint (sidebar should survive further down, or a compact rail between 768–900px). **Done:** both nav components regrouped into Today/Set up/Automate/Business (Insights already lives inside Assistant from an earlier merge; there's no separate "Booking page" route, the public page itself is edited via Settings, so those two audit labels map onto existing destinations rather than new ones). **Explicitly deferred, not silently dropped:** the three-tier 768/900px compact-rail breakpoint, and the setup/channel/trial/pending-change status badges — both are real net-new UI (a rail component; per-page-load status data threaded through the shared admin layout) that deserve their own pass rather than being bolted on here.
- [x] 8. Calendar day view → real time-grid (hour rows, duration-proportional blocks, overlap visibility); keep week view as compact overview; stronger mobile horizontal-scroll affordance. **Done:** `CalendarView.tsx` Day mode is now a genuine time-grid — hour rows on the left, blocks positioned/sized by real start time and duration, a same-day current-time line, and a standard sweep-line column layout so two overlapping bookings render side-by-side instead of stacking illegibly. Grid range defaults to 8am–8pm and widens to fit anything booked outside it (no business-hours data reaches this component yet, so this is the closest available signal — see note below). Week view untouched, exactly as the audit asked. Mobile horizontal-scroll affordance for Week view was already present pre-existing (`overflow-x-auto` + fixed min-width day columns); left as-is.
  - **Found in passing, not in the audit, flagging rather than fixing here:** `app/[slug]/admin/calendar/page.tsx` fetches *every booking the business has ever taken* with no time bound at all (unlike the dashboard, which was already fixed to a windowed query). Fine at a few dozen bookings, a real problem at a few thousand. Properly fixing it means re-fetching per navigation (Week/Day, Prev/Next) instead of the current load-once-filter-client-side model — a bigger change than this pass's scope. Left alone; worth its own pass.
- [x] 9. Booking form: persistent summary (`Service · Date · Time · Duration · Price · Deposit`), sticky on desktop / collapsible on mobile; explicit deposit-vs-full, refund/cancellation copy, "Review appointment" step before payment; explicit business timezone. **Done:** the existing step-2/3 summary bar now carries all six fields (was name+price+date/time only), is `sticky top-16` so it survives scrolling the calendar/slot list on any screen size (no separate mobile-collapsed variant needed - one sticky bar works at every width here), and shows the business's timezone abbreviation next to every time. Step 3 gained a one-sentence recap ("You're booking X with Y on Z") right above the submit button plus an explicit cancellation/reschedule policy line (new `cancellationWindowHours` prop, threaded from `booking_rules`) - both stated before commitment, not discoverable only after paying. Didn't add a literal fourth "Review" step/state - step 3 already renders the recap, deposit box, and policy line together above the submit button, which is the same guarantee (nothing charged without the full picture on screen) without the added flow risk of a new step. **Drive-by fix while in this file:** step 1 used `max-w-2xl` where steps 2-3 use `max-w-xl`, a real column-width jump the instant a service was picked - unified to `max-w-xl` throughout (this is item 9's "layout stability" note from the earlier, more detailed plan doc, not separately numbered here, but the same file/root cause).
- [x] 10. Consolidate owner login routes (`/login` vs `/[slug]/login`) into one canonical flow with preserved scoped redirects; visually separate owner vs customer (`/account/login`) auth identity; add shared auth-screen identity system (logo, role label, route back to public site). **Found already substantially done:** `/login` was already the canonical generic entry point (signs in, then asks the server which business the account belongs to via `/api/my-business`, redirects there) — `/[slug]/login` is deliberately kept as the scoped, bookmark-stable variant, not a duplicate; that split was already the right shape. **What was actually missing, now done:** new shared `AuthMark` component (small logo/name/role-label block) on all three auth screens, visible on mobile where the desktop branded panel disappears entirely (was a blank unbranded floating form); `/[slug]/login` gained a "Back to {business}" link (previously no way out but the browser back button); `/login` and `/account/login` now cross-link to each other ("Looking for a booking you made, not managing a business?" / "Run a business on Vanova?") so landing on the wrong one is a one-click fix instead of a dead end.
- [x] 11. Split Assistant into distinct jobs: ask-about-my-business (analytics) vs manage-my-schedule (pending changes/confirmations), with the "nothing changes until you say yes" trust message elevated. **Deliberately NOT done as literally written, and here's why:** Insights and Schedule Assistant used to be two separate tabs in this exact product, and earlier in this project the user explicitly said *"I dont see the use of separating it o / just have one for the bot"* - so re-splitting it into two experiences would directly reverse a recent, explicit instruction, not just miss an audit nuance. Implemented the underlying concern a different way: opening suggestion chips are now grouped under visible labels ("Ask" vs "Change something") so the two kinds of job are visually distinct from the first screen without splitting the thread itself, and the trust banner ("Nothing changes until you say yes") is now bolded, stronger-bordered, and separated from the page's usual warm-surface treatment so it reads as a guarantee rather than a passing tip. A literal split (separate confirmation-state UI, pending-request list, etc.) stays undone - that would need the assistant's API response to carry structured "this is a proposed change" metadata, which it doesn't today (`/api/assistant/chat` returns a plain reply string), and reversing the merge outright isn't this pass's call to make unilaterally.
- [x] 12. Mobile bookings list: replace compressed table with real appointment cards (date/time top, customer name prominent, service+duration, status badge, primary action row). **Done:** each row is now a genuine rounded/bordered/shadowed card on mobile (was a flat divided-list row) - `border-0` + conditional `border-b` keeps the existing plain-row desktop treatment exactly as it was, only mobile changed. Added the missing piece the audit called out by name: a primary action row (Message/Call, Mark completed, Cancel) via new local `RowActions`, shown only on bookings that are actually still actionable (upcoming + confirmed) - reuses the same `/api/bookings/[id]/status` route `NextAppointmentCard` uses. Kept to 3 actions rather than the full 5-button set on the single next-appointment card, since this repeats per row down a list that can run to dozens.
- [x] 13. Simplify Services controls — row hierarchy `name → duration → price → visible/bookable → edit`; move revenue/booking-count stats to a secondary panel; add-service as side panel/modal instead of inline-expand; price input needs currency context. **Found partially already true:** the row hierarchy already matches (`Service / Duration / Price / Status / actions`), and the Most-booked/Highest-revenue/Avg-duration stats already live in their own row below the table, not per-row clutter - so those two sub-items needed no change. **Actually fixed:** Add-service was an inline form that expanded above the table, pushing every row down the page (and back up on close); now a proper modal (`AddServiceModal`, reusing the existing `useDialog` focus-trap/Escape/scroll-lock hook the same way `NewAppointmentModal` does) so the table underneath never moves. Price inputs (both the add-modal and the inline edit row) now show a ₦ prefix instead of a bare number field.
- [x] 14. Hours: bulk editing (copy Monday→weekdays, apply Mon–Fri), multiple intervals per day (lunch break/split shift), holiday overrides, customer-visible-slot preview; stop saving one day at a time. **Done (the two things the item is actually named for):** verified first that `lib/slotGenerator.ts` already loops over every availability row for a day (`for (const window of hours)`) rather than assuming one - only the UI enforced a single window via delete-then-insert-one-row, so this was a frontend-only change with no backend/schema risk. `HoursManager` now supports any number of windows per day (add/remove, e.g. 9-12 + 14-18 for a lunch break) and a "Copy {day} to Mon-Fri" action that clones the selected day's open-state and windows onto Monday-Friday and saves all five immediately, rather than five separate manual saves. **Deferred, correctly out of this item's actual scope:** holiday/exception dates and a customer-visible-slot preview are new features (new data model for the first, a live availability preview UI for the second), not "bulk editing / multiple intervals" - noting rather than quietly skipping.
- [x] 15. Split Settings into first-class destinations: Booking page · Availability & policies · Payments · Integrations · Domain · Business profile — plus unsaved-change protection and clearer destructive-action warnings. **Deliberately NOT done as literally written, and here's why (same shape of call as item 11):** promoting Payments/Integrations/Domain to top-level nav items would directly reverse two things: the more detailed of the two audit docs (`UI_UX_AUDIT.md`) itself already flags this exact suggestion as wrong ("push back on that... Channels was deliberately extracted to its own page... recommend regrouping *within* Settings and keeping the separate pages"), and the user's own explicit correction earlier in this project - *"the dropdown was supposed to be on the nav tab under the settings tab"* - specifically rejected sub-items living at the top level. **What was real and got fixed:** the actual problem underneath the suggestion - "Booking rules" and "Payments" (a live Paystack secret key) shared one save button with a buffer-time number field and a Zapier webhook URL, so saving one silently risked the other. Split into two independent Settings sub-sections (`BookingRulesManager`, `PaymentsManager`), each its own component, own save action, own server round-trip - old combined `SettingsManager.tsx` deleted, both admin nav files updated (now 5 sub-sections: profile/content/rules/payments/domain). Added a small `useUnsavedChangesWarning` hook (`beforeunload` prompt when fields have diverged from what's saved) and wired it into both new managers. **Honest partial coverage:** the other three Settings managers (profile, content, domain) weren't retrofitted with the same warning in this pass. Checked for unconfirmed destructive actions elsewhere in Settings and found none (no delete/remove flow exists in `CustomDomainManager` today) - not a gap to fix, just verified.

## REFINE AFTER

- [x] 16. Reduce repeated rounded-card/dashed-border/soft-accent-pill visual pattern; assign radius by role (controls small, cards medium, tables minimal, pills/avatars full). **Scoped down, and here's the actual reasoning:** a full mechanical sweep of every `rounded-*` usage across the app (dozens of files) has no way to be visually verified in this environment - there's no browser/screenshot tool available here, so a blind find-and-replace risks introducing real regressions I can't see. Instead: wrote the radius-by-role convention down as a documented reference in `globals.css` (didn't exist anywhere before - the drift toward `rounded-2xl`/`rounded-3xl` for nearly everything had no written scale to drift *from*). Every new component built during this pass (`SetupChecklist`, `NextAppointmentCard`, `EmptyState`, `AddServiceModal`, `AuthMark`) already follows it naturally. Retrofitting the rest is incremental, not a one-shot sweep.
- [x] 17. Increase secondary text contrast where `--ink-soft`/`--ink-faint` are currently used for information that isn't actually low-priority. **Reviewed, no changes made:** `globals.css` already carries a deliberate, documented tradeoff for `--ink-faint` (~3.6:1 against paper/surface, below AA's 4.5:1 for body text, explicitly scoped to "the low-priority metadata/timestamp role this is actually used for site-wide, never body copy"). Across every component read and edited this session - prices, statuses, customer names, amounts due - the important information consistently uses `text-ink`/`text-accent`/semantic colors, not `ink-faint`; `ink-faint` shows up for exactly what its own comment says (timestamps, mono labels, secondary counts). No concrete violation found to fix rather than an unverified sweep for its own sake.
- [x] 18. Task-specific empty states across the app (not generic centered messages) — reuse the `AdminDashboardBody` pattern (what's missing + why it matters + an action). **Done, scoped:** extracted the pattern (icon + title + description + optional action) into a shared `EmptyState` component and applied it to the two genuinely weak ones - Staff (was a single plain sentence, now icon + "Flying solo for now" + an actual "Invite someone" action) and Billing (was a bare caption line). Checked Customers, Services, and the dashboard's own empty state first - all three already used the rich pattern independently before this component existed, so they needed no change (worth confirming rather than assuming from the audit's count). **Not touched:** Products (deliberately not in the primary nav, low value to polish further) and the Calendar week-view "Free" tiles (already an intentional minimal per-slot treatment, not a page-level empty state the pattern applies to).
- [x] 19. Strengthen public-page business identity: location/service area, open/closed status, starting prices, cancellation/payment expectations, better hero photography/crop, sticky mobile booking action. **Done, three of six:** new `isOpenNow` (in `lib/formatHours.ts`, computed server-side from the same availability rows `getBusinessBySlug` already fetches, in the business's own timezone) drives a real "Open now"/"Closed now" badge in the hero, not just the static weekly summary. Hero now shows a starting price ("From ₦X", derived from the business's own priced services) and, when payment is required, the deposit/full-payment expectation - both previously invisible until a customer picked a service three steps into the form. **Not built:** location/service-area (there's no address/location field in the schema at all - a new column plus settings UI, a real feature not a styling change) and hero photography/crop (a per-business asset choice, already noted under #2). **Sticky mobile booking action - reviewed, not duplicated:** the sticky header's mobile "Book now" button already stays visible on scroll at every screen size; a second sticky bottom bar would be redundant chrome doing the same job twice.
- [x] 20. Formal mini design system pass: buttons, icons (replace ad hoc hand-drawn SVGs with one consistent icon set/weight), status pills, radius, spacing — standardize and document. **Reviewed the icon claim specifically before acting on it:** every hand-drawn SVG read or written this session (well over 100, across dozens of files) already follows one convention - `fill="none" stroke="currentColor"`, stroke width consistently 1.6-2.2, no filled icons, no mismatched style creeping in. They're hand-drawn per use rather than pulled from a shared library, but they are NOT visually inconsistent the way the audit's wording implies. Swapping every one for an actual icon library (a new dependency, and hundreds of individual replacements with no visual tool to verify each one didn't shift alignment/sizing) is a real decision with a real cost, not a cleanup - that's the user's call to make, not mine to do silently in a redesign pass. Radius and status-pill conventions are covered by #16 (documented) and `lib/bookingStatus.ts` (already the single source, from earlier in this project). Spacing wasn't given a separate documented scale this pass - noting that as the one sub-item here with no concrete output.
- [x] 21. Marketing homepage: reduce repeated promise/copy across hero+preview+before/after+features+CTA; add real product proof (testimonial/example); stronger premium-plan visual distinction; don't point the live demo at a hardcoded tenant without labeling it a demo. **Done:** the "see it live" link already said "demo" in its own text, but that disclosure lived only on the homepage you left, not the `/glow-salon` page you land on - a visitor arriving there directly had no way to tell. `DEMO_SLUG` is now a shared constant (`lib/site.ts`, was a local const only the homepage knew about) and the public business page shows a small "Demo business - try booking, nothing's real" badge when the slug matches it. **Explicitly not done, flagging rather than guessing:** (1) trimming "repeated" copy across sections is a subjective copywriting call, and re-reading the actual sections (before/after, how-it-works, features, who-it's-for) they're distinct in function even if they share an underlying promise - without a visual tool to check what a change actually does to the page, and given [[Design restraint]], rewriting a conversion-facing page on my own literary judgment isn't the right call to make unilaterally; (2) "add real product proof" needs an actual testimonial or customer example - **I don't have one and won't fabricate one**, that has to come from you; (3) premium-plan visual distinction wasn't touched this pass.

## PAGE-BY-PAGE (folded in above where covered; tracked individually so nothing is missed)

- [x] 22. `/signup` — password visibility toggle, explicit requirements, slug-availability live check, "your customers will visit vanovahub.com/your-business" preview, terms/privacy line, keep value panel present on mobile, move form higher on viewport. **Done, most of it:** password show/hide toggle added; new debounced (450ms) live slug-availability check (`GET /api/signup/check-slug`, a read-only check distinct from the real create-account validation that still runs server-side) - "/name is available" or "already taken" now shows while typing, not only after a failed submit with email+password already filled in; mobile now gets the same `AuthMark` brand block the login screens got in #10, instead of a blank unbranded form. The domain-prefixed input field (`{host}/` + slug, live) already served as the "your customers will visit…" preview before this pass - confirmed rather than re-added. **Not done, flagged rather than faked:** there is no Terms of Service or Privacy Policy page anywhere in this app - I'm not going to author placeholder legal text and link to it as if it were real legal protection for a product that collects payment info and customer PII. That needs an actual decision from you, not something to improvise into a redesign pass. "Move form higher on viewport" wasn't touched - centered placement is a minor, subjective call not worth the risk without a way to see the result.
- [x] 23. `/account` hub — "Book again" from empty state, stronger Manage/message action hierarchy, grouping for multi-business customers, add-to-calendar/address/payment/cancellation-deadline visibility, stronger state differentiation (upcoming/past/cancelled/completed). **Found already true:** Upcoming/Past grouping already existed; status differentiation already used `statusStyle`'s distinct colors per state plus a strikethrough for cancelled. **Done:** "Manage" upgraded from a small text link to a real button (it's the actual primary action - reschedule/cancel - and read as an afterthought next to the status pill); added "Add to calendar" (extracted `googleCalendarUrl` out of `BookingForm.tsx` into shared `lib/googleCalendar.ts` rather than duplicating it) for upcoming, non-cancelled bookings; price now shows on the card (the query already fetched it, just wasn't rendered); empty state upgraded via the shared `EmptyState` component. **"Book again" - deliberately reframed, not built as literally asked:** Vanova has no directory of businesses to browse, so a "book again" catalog doesn't exist to link to; the empty state instead explains honestly how a booking actually gets here rather than pointing at a discovery flow that isn't real. **Not done:** address and cancellation-deadline per booking would need an extra `booking_rules` join per business shown on the page - deferred rather than adding that query complexity in this pass.
- [x] 24. `/[slug]/about` — add location, team, specialties, hours, booking CTA, social proof; stop reading as a placeholder text page. **Done:** `hoursSummary` was already being fetched for this page's footer but never actually shown in the page content itself - now displayed; added a "Book an appointment" CTA, which didn't exist anywhere on the page before. **Not done - real data gaps, not styling:** location, team, and specialties have no schema fields to read from; social proof needs an actual testimonial (same limitation as #21, won't fabricate one).
- [x] 25. `/[slug]/gallery` — captions, categories, hero image, booking CTA, intentional sparse/empty state. **Done:** added a booking CTA below the grid; `GalleryGrid` rendered nothing at all for zero images (a page titled "Gallery" over blank space, which reads as broken, not sparse) - now shows an honest "No photos here yet" state. **Not done:** captions and categories need a schema change (`gallery_urls` is just an array of URL strings today, no per-image metadata) - a real feature, not a styling gap; hero image reuses the existing cover photo already (About page's pattern), not duplicated here since the gallery IS the photo content.
- [x] 26. `/[slug]/contact` — address/map, hours, expected response time, booking CTA, prominent action in the no-contact-details empty state. **Done:** "We'd love to hear from you" (pure filler) replaced with the actual `hoursSummary` when available - same already-fetched-but-unused data as About; added a booking CTA; the no-contact-details state upgraded from a single sentence to the shared `EmptyState` with two real buttons (Book / Chat), not just text mentioning that those exist. **Not done:** address/map needs a location field that doesn't exist in the schema (same gap noted in #19); "expected response time" isn't something I can state honestly per business without inventing an SLA no one configured.
- [x] 27. `/[slug]/manage/[bookingId]` — lead with "what can I do with this booking now," show price/payment state and deadlines, give cancelled bookings a next-best action. **Found the P0 correctness work here already done** (loading state during slot fetch, distinct error-vs-empty availability, past/cancelled gating) - not re-touched. **Done this pass:** price and payment state now shown on the card (was completely absent - a customer checking a paid booking had no way to confirm the charge went through from this page), with the same `42703`-migration-not-run defensive fallback pattern used everywhere else in this codebase; a real cancellation-deadline line ("Free to cancel or reschedule up to X hours before"); cancelled and already-happened bookings now offer a "Book a new appointment"/"Book again" action instead of a dead-end card with nothing to do next.
- [x] 28. `/[slug]/forgot-password`, `/reset-password`, `/accept-invite` — show business identity, invited email, expiry state, recovery action for bad/expired links, route back to login. **Found already true:** `AcceptInviteForm` already showed the invited email and business name once the invite loaded, and raw Supabase errors were already routed through `friendlyError` on both password forms - not re-touched. **Done:** all three screens now carry the `AuthMark` business-identity block (forgot/reset-password fetch the real business via `getBusinessBySlug`, matching `/[slug]/login`'s pattern); "Missing invite token" and "invalid or already used" were both flat dead-end sentences - both now explain what happened and link to `/${slug}/login`; forgot-password gained an explicit "Back to login" link. **Not done:** there is no `expires_at` column on `staff_invites` at all - "expiry state" isn't something the data model tracks today, so distinguishing "expired" from "already used" would mean adding a real column/migration, not a copy fix.
- [x] 29. Admin Customers — real profile fields: appointment count, last/next appointment, total spend, preferred contact, notes; move off pure booking-row derivation where practical. **Found most of this already built:** appointment count, total spend, last visit, and preferred contact (with click-to-message for bot contacts) were already computed by `CustomersManager`'s existing aggregation - this page was more mature than the audit's characterization suggested. **Done:** added the one genuinely missing field, "next appointment" - the aggregation only tracked *last* visit before, so a returning customer with something already booked looked identical to one who might never come back. Shows as a small accent badge next to their name. **Correctly not "fixed":** the unbounded query here (loads every booking ever) is right for this page's actual job - lifetime customer stats - unlike the Calendar page's identical-looking query flagged in #8, which only ever needs a viewable window; bounding this one would silently undercount a returning customer's true history. **Notes and "move off booking-row derivation":** a real customer-notes feature and a genuine customers table are both new schema/architecture, not a redesign-pass change - correctly out of scope, not silently dropped.
- [x] 30. Admin Staff — surface role, availability, appointment ownership, permissions, active/inactive, pending invites on the page itself, not just the invite mechanic. **Done:** appointment ownership - "X upcoming" now shows next to each team member, bounded to non-cancelled future bookings (a new query in the page, wasn't fetched before); pending invites already lived on this page, not just "the invite mechanic" elsewhere - confirmed, not re-added. **A real finding surfaced while checking the "permissions" sub-item, not something to gloss over:** I started to add a tooltip explaining what each role can/can't do, then checked it against the actual authorization code first - `requireStaffSession`/`requireStaffApiSession` only verify a staff row exists for the business, never branch on its `role`. Every invited staff member today has the exact same access as the owner: billing, removing other staff, everything. `role` is currently a label with no enforcement behind it. **I did not ship a tooltip describing permissions that don't actually exist** - that would have told business owners their team is scoped when it isn't. This is worth your own attention regardless of this redesign pass: either the product should enforce role-based access, or the "role" label/invite flow should stop implying it already does. **Not done - real feature gaps, not styling:** per-staff availability (the `availability` table's `staff_id` column already supports this structurally, but there's no UI for it - would mean duplicating `HoursManager` per staff member, a substantial feature); active/inactive state (no such column exists on `staff` today - a row exists or it doesn't, there's no "disabled but not removed" state to build a toggle for).
- [x] 31. Admin Channels — per-channel state model: connected/not, what customers can do through it, last-tested status, setup action, test-conversation action, example experience. **Found already true:** connected/not-connected state, the failed-fetch-renders-as-disconnected bug, and "Website chat" itself being listed as a channel were all already done in an earlier pass - not re-touched. **Done - real "test it now" actions, not descriptions of one:** Website chat's "Set up" button was permanently disabled and did nothing; now links to `/${slug}#chat` to try the live widget. Telegram, once connected, now links to `https://t.me/{username}` - the business's own bot, a genuine working chat. WhatsApp links to `https://wa.me/{number}` (stripped to digits, since the display number can arrive with a leading + and spaces). **Deliberately not added for Messenger:** the connected value there is a Facebook page *display name* (e.g. "Glow Salon"), not a URL-safe page username - `m.me/{pageName}` would very likely be a broken link for any page name with a space or special character in it. Rather than ship a link that might not work, I left it alone. **Not done:** last-tested timestamps and "what customers can do" example copy weren't added - the former needs a new column to actually track, the latter risks reading as generic filler without something real to say per channel.
- [x] 32. Admin Billing — account summary: current plan, renewal/trial date, payment method, status, upgrade action, feature comparison, payment history, expiration consequences. **Found this page already substantially complete** - more than any other item in the whole list, this one didn't need building: current plan + price, a real status pill for every phase (active/trial/cancelling/past_due/expired/none) with the actual renewal, trial-end, or access-end date per phase, upgrade/subscribe/cancel actions, a persistent plan blurb doubling as feature description, an explicit switch-plan hint, full payment history, and the expiration-consequences line ("your trial has ended, so the rest of the dashboard is paused") were all already there, plus the same `42703` defensive-migration pattern used everywhere else. Only touched this pass to upgrade its empty payment-history state to the shared `EmptyState` component (#18). **Genuinely not buildable in this pass:** payment method (card/bank details) - Flutterwave hosted checkout means this app never stores or sees that data by design (correct for PCI reasons), so showing it would mean a new integration against Flutterwave's own API to fetch it, not a display change.
- [x] 33. Resolve `/[slug]/book` route/documentation mismatch (route doesn't exist but is referenced as part of the product). **Done:** `PRODUCT_FEATURES.md` documented this as "a dedicated booking route... intended for direct booking links" - a real spec/app mismatch, not just missing polish. Made reality match the doc rather than the other way around: new `app/[slug]/book/page.tsx` redirects to `/${slug}#book`, skipping the marketing hero and landing directly on the booking form, which is what "a direct booking link" actually needs without duplicating the whole booking flow behind a second URL.

---

## Follow-up pass (after the 33/33 above)

Three items were explicitly deferred during the main pass (the tablet nav breakpoint and status indicators in item 7; booking-row click-through wasn't in the original 33 but was raised during a reconciliation pass). All three are now done:

- [x] **Tablet compact rail (768–900px).** `AdminSidebar.tsx` previously jumped straight from the full mobile hamburger menu (`AdminMobileNav`, below 768px) to the full 256px sidebar (768px+) - a business owner on an iPad or a small laptop window got the mobile experience despite having room for persistent nav. New 72px icon-only rail fills that gap: same destinations, same active/badge state, no group labels or business name (no room at that width). Full sidebar's breakpoint moved from `md:flex` to `min-[900px]:flex`; the rail fills `md:flex min-[900px]:hidden`; `AdminMobileNav` unchanged (`md:hidden`, still owns below-768px).
- [x] **Nav status indicators** (setup incomplete, channel disconnected, trial ending). Computed once in `app/[slug]/admin/layout.tsx` (shared by every admin page) from real data - the same three signals `SetupChecklist` uses for setup, whether any of Telegram/WhatsApp/Messenger is connected, and whether the subscription is in its trial phase with ≤3 days left. Renders as a small warning-colored dot on the relevant nav item (Dashboard/Channels/Billing) in the full sidebar, the compact rail, and the mobile menu - plus one aggregate dot on the mobile menu's own toggle button, so "something needs attention" is visible without opening the menu. **"Pending schedule changes" was not built** - the AI assistant's confirm-before-change flow is entirely conversational; there's no persisted "proposed change awaiting confirmation" record anywhere to query. Building that indicator would mean giving the agent loop a new schema to persist pending proposals, not a nav change.
- [x] **Booking rows clickable.** `BookingsList` rows previously opened nothing except the contact link; a mobile-only 3-button `RowActions` subset was a stopgap. Extracted `NextAppointmentCard`'s full detail+actions content into shared `BookingDetail.tsx` (used by both `NextAppointmentCard`, unchanged in behavior, and new `BookingDetailModal.tsx`), then made every row in `BookingsList` clickable (`role="button"`, keyboard-accessible) to open that same full action set - Message/Call, Reschedule, Mark completed, No-show, Copy reschedule link, Cancel for anything still upcoming; just a Message action for history. `RowActions` deleted as redundant now that the row itself does more than it did.

All three verified with `tsc --noEmit` + `next build` + the test suite, same as every item above.

## Second follow-up pass (the pasted transcript had a second half)

The live-browser transcript the user pasted got cut off partway through and was re-pasted in full later, with "and you're sure you made all the changes?? ... and the other ones I pasted?" - a fair challenge, since the second half covered Calendar/Customers/Services/Hours/Staff/Channels/Assistant/Billing/Settings tabs, the mobile audit, and a recommended order, and most of it does restate things already fixed above. Went through it item by item rather than assuming; four genuinely new, unaddressed items surfaced:

- [x] **Customers: no way to see a customer's actual history.** Rows showed aggregate numbers (visits/spend/last visit) but clicking did nothing except the contact cell. New `CustomerDetailModal.tsx` - visit count, total spent, last visit, and the customer's full booking history (filtered from the same `bookings` data the page already loaded, no new query), plus a message action. Rows are now clickable the same way BookingsList's already were.
- [x] **Bookings list tabs mixed a time scope with a status filter in one row** ("All" / status pills / "Past" as if they were equivalent choices) - exactly what the audit called out. Split into two independent rows: a 3-way scope toggle (**Upcoming / Today / Past** - added Today as its own scope, not just implied by the stats strip elsewhere) and a separate status-filter row underneath that narrows whichever scope is active.
- [x] **Hours had no customer-facing preview.** New example-slot preview under each day's windows (`previewSlots` in `HoursManager.tsx`) - pure wall-clock formatting in 30-minute steps, explicitly labelled "Example," since the real slot list depends on which service's own duration a customer picks (that's `lib/getAvailableSlots.ts`'s job, not this preview's).
- [x] **Channels had no "what customers can do" copy**, which I'd previously deferred as risking generic filler - reconsidered: a one-line, factually accurate description of what the AI receptionist actually does ("ask about availability, get recommendations, and book directly in the chat") isn't filler if it's true and specific, so it's now under each channel's heading (Telegram/WhatsApp/Messenger). Per-channel "last activity" timestamps still need a real new column - not built.

**Confirmed still NOT done, on purpose, unchanged from before:** Terms of Service/Privacy Policy, a real testimonial, and reversing the Assistant-merge or Settings-nav-placement decisions. **One small thing not enhanced:** the Calendar week view's mobile horizontal-scroll affordance - still the pre-existing `overflow-x-auto` with no added scroll cue.

All four verified with `tsc --noEmit` + `next build` + the test suite.

## Third pass — "follow everything from the suggestions... do am nau"

The user explicitly asked to now implement the two items deliberately done *differently* earlier (with an explicit "if it doesn't work, we revert" safety net - and since nothing in this whole session is committed, a revert is trivial). Both are now done as literally suggested:

- [x] **Assistant split back into two destinations.** The merged `/admin/assistant` page + `/api/assistant/chat` endpoint (and the underlying agent) are untouched and still work - AskAssistantBar on the dashboard still routes there. But the primary nav no longer has one "Assistant" item; it has two, restored from routes that already existed as redirects (`/admin/insights` → `runInsightsAgent`/`/api/insights/chat`, gated by the Business Intelligence plan with a real upgrade prompt when not enabled; `/admin/schedule-assistant` → `runRescheduleAgent`/`/api/schedule-assistant/chat`, ungated). Both underlying agents and endpoints had survived the original merge untouched, so this was mostly wiring, not new logic.
- [x] **Settings sections promoted to top-level nav items.** The 5 sections (Business profile, Website content, Booking rules, Payments, Custom domain) no longer hang off one "Settings" item, nested and hidden until you're already there - they're each their own line in the Business group now, same `/admin/settings?section=X` routing underneath. **Worth being explicit: this reverses the user's own earlier correction from this same session** ("the dropdown was supposed to be on the nav tab under the settings tab") - done anyway because they explicitly asked to follow the suggestions literally this time, with the stated fallback of reverting if it doesn't feel right.
- [x] **Calendar week view mobile scroll cue.** Added a fade-edge hint (mobile only) that more days sit off-screen to the right, on top of the partial-column peek that already existed.

**Still not done, and here's the line I'm holding regardless of "do it now":**
- **Terms of Service / Privacy Policy** - still not drafting placeholder legal text presented as real protection without being asked for that specifically.
- **A customer testimonial** - still won't invent a person/company that doesn't exist to praise the product. This one isn't a "might not work, we'll revert" experiment - a fabricated review is deceptive the moment it's live, not just until reverted.
- **Per-channel "last activity" timestamps** - stopped short here for a different reason than principle: this needs a real schema change (new columns) run against the live Supabase database, plus edits to the actual webhook handlers that process real customer messages (Telegram/Meta). That's a materially different risk category from every UI change in this document - a live DB migration and production webhook logic aren't something to charge through on a general "do everything" instruction without calling it out specifically first. Flagging for an explicit go-ahead rather than doing it quietly.

All verified with `tsc --noEmit` + `next build` + the test suite.

## Working notes

- Verification per item/batch: `npx tsc --noEmit`, route sweep, and (for anything touching booking/payment) reasoning through the real data path against live Supabase.
- No commit/push without explicit instruction (standing project constraint) — changes land locally; I'll flag when a batch is ready to ship.
- Will report progress after each completed item or tightly related group, not go silent for the whole list.
